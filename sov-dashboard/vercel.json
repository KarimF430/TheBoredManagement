{
  "//": "Functions MUST run next to the database. Supabase is in ap-southeast-1 (Singapore), so sin1 keeps DB round-trips at ~1-2ms. Vercel's default (iad1, Washington DC) puts every query on a trans-Pacific hop, and routes like /api/dashboard issue 21 of them.",
  "regions": ["sin1"],
  "crons": [
    {
      "path": "/api/warm",
      "schedule": "0 7 * * *"
    },
    {
      "path": "/api/cron?job=daily_views",
      "schedule": "0 8 * * *"
    },
    {
      "path": "/api/cron?job=sheets_sync",
      "schedule": "0 9 * * *"
    }
  ]
}
